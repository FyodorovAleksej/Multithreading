package by.fyodorov.multithreading.port;

/**
 * enum with ways to creating port:
 * DEFAULT - empty port with default parameters (DefaultBuilder)
 * FILE - port from file (FileBuilder).
 */
public enum PortBuilderEnum {
    DEFAULT,
    FILE
}
